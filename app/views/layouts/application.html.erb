<!DOCTYPE html>
<html>
  <head>
    <title>GossipPostgresqlViews</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= favicon_link_tag asset_path('favPirate.png') %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js" integrity="sha384-QJHtvGhmr9XOIpI6YVutG+2QOK9T+ZnN4kzFN1RtK3zEFEIsxhlmWl5/YESvpZ13" crossorigin="anonymous"></script>
  </head>

  <body class="text-center">
    

  <nav class="navbar navbar-expand-lg navbar-light bg-light ">
  <div class="container-fluid center">
    <a class="navbar-brand" href="<%=gossips_path%>">THP_Rescapés</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
       <li class="nav-item">
          <a class="nav-link" href="<%=gossips_path%>">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="<%=team_path%>">Team</a>
          <%# class="nav-link active" %>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%=contact_path%>">Contact</a>
        </li>
      </ul>
      <ul class="navbar-nav justify-content-end">

      <li class="nav-link">
    <% if session_en_cours? %>
       <%= link_to "Bienvenue " + current_user.first_name,  {controller: "users", action: "show", id: current_user.id}, :class => "nav_item active" %>
    <% else %>
      <%= link_to "S'inscrire", new_user_path, :class => "nav_item active" %>
     <% end %>  
      </li>

    <li class="nav-link">
    <% if session_en_cours? %>

      <%= link_to "Se déconnecter", session_path(session.id), method: :delete , :class => "nav_item active" %>
    <% else %> 

      <%= link_to "Se connecter", sessions_new_path, :class => "nav_item active" %>
    <% end %> 
      </li> 
    </ul> 
    </div>
  </div>
</nav>

    <% flash.each do |key, value| %>
    <div class="flash <%= key %>" style="background-color:beige"><%= value %></div>
    <% end %>

<div class="container" >
<%= yield %>
</div>
  </body>
</html>
